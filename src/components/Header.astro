---
import { brand } from "../brand.js";

const { locale = "en" } = Astro.props;
const isZh = locale === "zh";
const prefix = isZh ? "/zh" : "";
const guide = isZh ? "/zh/guide/" : "/guide/";
const swap = isZh ? "/" : "/zh/";
---

<header class="topbar">
  <a class="brand" href={`${prefix}/`} aria-label={`${brand.product} homepage`}>
    <span class="brand-mark">D</span>
    <span>{brand.product}</span>
  </a>
  <nav aria-label="Primary navigation">
    <a href={`${prefix}/#features`}>{isZh ? "功能" : "Features"}</a>
    <a href="/download/">{isZh ? "免费下载" : "Download free"}</a>
    <a href={guide}>{isZh ? "指南" : "Guide"}</a>
    <a href="/feedback/">{isZh ? "反馈" : "Feedback"}</a>
    <a href={`${prefix}/#faq`}>FAQ</a>
    <a class="language-link" href={swap}>{isZh ? "EN" : "中文"}</a>
    <a class="nav-buy" href="/buy/">{isZh ? "查看价格" : "View pricing"}</a>
  </nav>
</header>
